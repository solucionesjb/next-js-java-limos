import type { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Configuraciones',
    description: 'Página de configuraciones',
    
};

export default function ConfiguracionesPage() {
    return <div>Configuraciones Page</div>;
}